<h1><%= @recipe.name %> Recipe</h1>
<table>
  <th>Ingredient Name</th>
  <th>Amount in ounces</th>
  <% @recipe.recipe_ingredients.each do |recipe_ingredient| %>
  <tr>
    <td><%= recipe_ingredient.ing_name %></td>
    <td><%= recipe_ingredient.amount %> <%= link_to 'Edit Recipe',
              edit_recipe_ingredient_path(@recipe) %> </td>
  </tr>
  <% end %>
</table>

<%= link_to 'Add Ingredient', new_recipe_ingredient_path(@recipe) %><br><br>
<%= link_to '<< Back to Recipes', recipes_path %>